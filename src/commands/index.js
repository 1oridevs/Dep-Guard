const scan = require('./scan');
const analyze = require('./analyze');
const interactive = require('./interactive');
const ci = require('./ci');

module.exports = {
  scan,
  analyze,
  interactive,
  ci
}; 